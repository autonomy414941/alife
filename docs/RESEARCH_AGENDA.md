# Research Agenda

## Current Direction
Over the next month, shift from adding richer founder-context matches to decomposing what the current gains actually mean. Prioritize a second null family that does not inherit realized species histories, so the project can separate upstream species-generation gains from downstream clade-structuring gains before another mechanism sweep. In parallel, keep shrinking the study/runtime surfaces that this evaluation work will keep reopening, especially the founder-establishment study family and the simulation founding loop.

## Why This Direction
The March 15, 2026 founder-crowding validation is now in code, tests, and artifacts, and founder grace still improves the static-habitat baseline under the stricter habitat-plus-crowding null (`+9` active-clade delta improvement at cladogenesis threshold `1.0`, `+4` at `1.2`). That means the immediate blocker is no longer "does one more founder-context control erase the gain?" The blocker is that every tested variant still loses to its null in absolute active clades (`-25.75` for founder grace under habitat-plus-crowding matching; best current ecology gate `-17` with a large persistent-activity regression), while the matched null still repartitions realized species histories and therefore cannot distinguish upstream innovation from clade relabeling. Recent refactors already extracted study-result builders, relabel-null runners, and evolution-history helpers, so the highest leverage now is cleaner evaluation plus the next targeted splits.

## Structural Constraints
`buildMatchedSchedulePseudoClades()` still constructs pseudo-clades by repartitioning realized `species` histories, so any intervention that improves species origination timing, abundance, or persistence is partly inherited by the null. `src/activity.ts` (1860 lines), `src/simulation.ts` (2192 lines), `src/clade-activity-relabel-null-new-clade-establishment-horizon-study.ts` (689 lines), and `src/types.ts` (1223 lines) still concentrate too many responsibilities, so each evaluation change reopens the same large files. The simulator also still accumulates full `TaxonHistory.timeline` histories and per-tick `localityFrames`, which keeps long-horizon validation expensive and raises the cost of any future counterfactual or genealogy work.

## Revision History
- 2026-03-14: Set the month on founder-establishment evaluation after habitat coupling and founder grace became the only durable horizon wins.
- 2026-03-14: Shifted from more scalar founder-protection sweeps toward stricter matched-null controls and dead-axis pruning after ecology gating improved active clades but sharply regressed persistent activity.
- 2026-03-14: Added study-surface cleanup and structural decomposition after duplicated wrappers and brittle artifact emission started slowing iteration.
- 2026-03-14: Revised again toward richer founder birth-context controls plus monolith splits because habitat-bin matching and encounter-restraint archiving were done, but the system still trailed the matched null and the core analysis/runtime files remained oversized.
- 2026-03-15: Pivoted from adding another founder-context control to decomposing species-conditioned-null leakage because habitat-plus-crowding matching is now validated, founder grace still remains far below null in absolute active clades, and recent refactors already cleared the path for a cleaner evaluation pass.
