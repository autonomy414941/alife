# Backlog

- [validate] Isolate movement-only, reproduction-only, harvest-only, and combined behavioral policies under refined attribution to measure per-surface harvest, survival, reproduction, and gating deltas against memoryless controls
- [validate] Sweep movement and reproduction threshold regimes with refined attribution and gate-specific observability to map activation rates against fitness outcomes and test whether the March 21, 2026 detriment was parameter-specific
- [feat] Extend behavioral control to encounters: add contingent aggression policies (e.g., retreat-after-loss, conditional aggression based on energy state)
- [validate] Run matched baseline comparing behavioral-control-enabled runs against memoryless default to measure diversification and complexity gains
- [validate] Extend normalized GenomeV2 distance revalidation from the 500-step subset to a bounded 4000-step, 2-4 seed panel if full-horizon robustness still matters for direction-setting
- [validate] Add per-trait fitness decomposition to ecological-context validation: track harvest intake, survival probability, and reproductive output conditional on trait presence vs absence within matched ecological bins
- [validate] Re-run encounter-topology x composition-cost factorial with live GenomeV2 after canonical validation and normalization land
- [expand] [Environmental Complexity] The world is a static fertility map plus one fungible resource pool with exogenous disturbance; agents can deplete and recycle resources but cannot persistently construct, poison, shelter, or otherwise rewrite habitat. Intervention: add persistent per-cell state layers that agents deposit, erode, and sense over many ticks, then feed those layers back into movement, harvest, and survival.
- [expand] [Trait Decoder Bottleneck] `GenomeV2` can store arbitrary trait keys, but the live simulator still routes ecology through hard-coded reads of `metabolism`, `harvest`, `aggression`, `harvestEfficiency2`, and taxon-level habitat / trophic / defense caches. Under this architecture any newly added locus is selectively inert baggage until a human wires it into a specific operator, so schema expansion alone cannot yield new phenotypes or interactions. Intervention: replace per-operator hard-coded trait access with a trait-expression registry or phenotype decoder that maps declared loci into metabolism, harvest, encounter, movement, settlement, and taxonomic decisions. Dimension: Expressiveness, Assumptions.
- [expand] [Interaction Richness] The interaction alphabet is compile-time fixed to movement, harvest, same-cell energy theft, reproduction, and thresholded clade founding, so lineages cannot evolve new interaction types such as signaling, cooperation, public goods, or alternative predation / resource-conversion modes. Intervention: generalize encounters and foraging into evolvable action channels over multiple resource or field layers instead of a single hard-coded transfer equation.
- [expand] [Inheritance Architecture] Every descendant is created by single-parent clonal mutation, with speciation and cladogenesis as irreversible threshold side effects of that one lineage. Within this framework the system cannot express recombination, sexual selection, mating compatibility, or horizontal trait transfer, so innovation is limited to local mutation walks around one parent genome. Intervention: introduce multi-parent inheritance or exchange operators plus compatibility / partner-choice rules, and make parentage an explicit part of descendant creation.
- [expand] [Evolutionary Mechanisms] Speciation and cladogenesis are single-offspring threshold events in a wrapped torus with no barriers, demes, mate choice, or lineage-specific breeding structure, so the system cannot express allopatric isolation, reproductive barriers, or adaptive radiation driven by sustained separation. Intervention: add patch barriers or a patch graph plus lineage-local colonization / breeding pools, and require sustained ecological or spatial separation before promoting a new clade.
- [feat] [Descent Observability] `TaxonHistory` retains only per-taxon counts and timelines; the simulation discards parent-child edges, founder trait snapshots beyond a clade founder genome, and phenotype transitions at speciation / cladogenesis time. That makes cumulative novelty, convergence, evolutionary dead ends, and innovation reuse structurally unmeasurable no matter how the Planner reshapes current studies. Intervention: persist a lightweight genealogy / innovation graph with parent ids, founder traits, and phenotype deltas at taxon birth events, then derive novelty and reuse metrics from that graph.
- [validate] [Temporal Credit Assignment] The study surface mostly aggregates final snapshots, fixed-window turnover, and persistence-threshold deltas, so interventions that create repeated but short-lived innovations or long transient scaffolding before late collapse are nearly invisible to the current optimization loop. Under the present framework the system can optimize endpoint persistence proxies, but not ratcheting search over trajectory quality. Intervention: add first-class trajectory metrics such as area under active-clade diversity, innovation survival curves, regime-switch counts, and recolonization-time distributions alongside the existing final-state summaries.
- [refactor] [Trait Granularity] Habitat preference, trophic level, and defense level live in taxon-level scalar maps and are only updated on speciation or clade founding, while movement, harvest, and encounters consume those taxon summaries instead of individual phenotype. That makes within-species ecological polymorphism, frequency-dependent morph coexistence, and gradual niche divergence inside an extant species structurally impossible; ecological differentiation only becomes visible after a taxonomic split. Intervention: move ecological phenotype fields to heritable per-agent state updated every birth, and derive species / clade summaries from those distributions instead of using taxon-level lookup tables as the source of truth.
- [expand] [Dispersal Kinematics] Adults can only stay put or move one cardinal step per tick, and offspring can only settle in the parent cell or its four cardinal neighbors; `dispersalRadius` changes crowding calculations, not move or settlement distance. No amount of parameter tuning can produce rare long-range colonization, dispersal polymorphisms, migration fronts with variable jump lengths, or metapopulation rescue via occasional distant propagules. Intervention: separate sensing radius from movement / settlement kernels, then add configurable or evolvable step-length and offspring-dispersal distributions including rare long-distance events.
- [validate] [Taxonomic Proxy Leakage] The optimization surface still scores success mainly through species / clade counts, persistence windows, turnover, and relabel-null deltas, but taxon identities are minted by threshold crossings rather than by demonstrated ecological distinctness. That means the Planner can win on paper through ecologically near-identical taxonomic splits or by preserving labels without opening new niches, and the current summaries will often treat that as diversification. Intervention: add ecology-weighted diversity metrics that discount near-duplicate taxa, such as phenotype-distance-weighted richness, effective niche occupancy, and interaction-distinct clade counts, and make those co-equal with raw taxon counts in study decisions.
- [validate] [Matched-Null Fidelity] The relabel-null baseline preserves clade birth counts by tick, then randomly reassigns species into pseudo-clades without preserving founder habitat, local crowding, abundance class, or disturbance context. Under that proxy, a stack can look better than null while still failing to create genuinely distinct ecological opportunities. Intervention: add stricter null families that also match founder habitat bin, local occupancy / lineage crowding, abundance class, and disturbance phase at birth before treating delta-vs-null gains as evidence of progress.
- [feat] [Mechanistic Causality] `TaxonHistory` tracks births, deaths, and population counts, but the system does not retain cause-specific losses, harvest intake, predation transfers, movement costs, or disturbance impacts by species / clade. That makes it structurally impossible to tell whether a reported gain came from better founding, lower same-lineage crowding, reduced predation, or simple resource hoarding. Intervention: add per-taxon demographic and energy ledgers with cause tags, then surface founder-phase and maintenance-phase budgets in the study layer.
- [expand] [Finite Locus Catalog] `mutateGenomeV2()` only adds loci from a compile-time `candidateNewLoci` list, `EXTENDED_TRAITS` is human-enumerated, and the current adapter path still permits only `harvestEfficiency2` during live reproduction. That means the apparent extensible genome still cannot invent unforeseen trait kinds no matter how long evolution runs; it can only toggle a small human-approved menu. Intervention: replace fixed candidate-name lists with a trait-module factory or parameterized locus generator that can mint new loci families and persist their metadata in heritable state. Dimension: Expressiveness.
- [refactor] [Shared Scalar Geometry Assumption] `GenomeV2` currently models every locus as a bounded scalar with the same mutation operator, default fallback, and unweighted L1 distance, even though existing physiology already uses heterogeneous trait scales and would need categorical, thresholded, or structured state for richer mechanisms. Within that geometry, added complexity is limited to "more numbers in a map," while speciation and cladogenesis remain sensitive to arbitrary scaling choices rather than semantic novelty. Intervention: introduce per-locus metadata for domain, bounds or transforms, mutation kernel, and distance weight, and allow typed loci or small modules instead of only `string -> number` scalars. Dimension: Expressiveness, Assumptions.
- [validate] [Legacy Metric Surface] `StepSummary`, CSV exports, and aggregate experiment outputs still hard-code mean and selection-differential reporting to `metabolism`, `harvest`, and `aggression`, while strategy analytics remain restricted to habitat / trophic / defense summaries. If new loci begin evolving, the main feedback loop cannot see whether they spread, matter, or drive novelty, so Planner optimization remains locked to the legacy triad even after representational expansion. Intervention: add generic trait-distribution, loci-count, add/remove event, and trait-expression metrics to run summaries and study exports, then make them co-equal with taxon-count outcomes in experiment decisions. Dimension: Feedback Loops.
- [expand] [One-Dimensional Habitat Axis] `effectiveBiomeFertilityAt()` exposes habitat as a single scalar and `habitatMatchEfficiency()` compares lineages against that one value, while `buildBiomeFertility()` generates only one underlying abiotic field. Even if GenomeV2 adds richer habitat loci, the current world can only reward specialization along one productivity gradient, so orthogonal abiotic tradeoffs such as wet vs dry, warm vs cold, exposed vs sheltered, or substrate-type mosaics are structurally impossible. Intervention: replace the single fertility field with a vector of abiotic layers and let habitat phenotype encode multi-axis tolerance or preference over that field bundle. Dimension: Expressiveness.
- [expand] [Diet-Choice Compression] `resolveDualResourceHarvest()` and `combinedResourceAvailability()` force each agent to consume and value the two resource layers in a fixed ratio derived from `genome.harvest` and `harvestEfficiency2`. Under that architecture agents cannot conditionally switch diets based on local scarcity, internal pool deficits, disturbance phase, or competitor presence; selective foraging is reduced to a static weighted average baked into morphology. Intervention: separate substrate preference / effort allocation from processing efficiency, add a harvest-choice policy that can reallocate effort across resource layers from observations and internal state, and make movement / settlement score candidate cells against those chosen allocations instead of one hard-coded mix. Dimension: Expressiveness, Assumptions.
- [validate] [Energy-As-Fitness Proxy] `selectionDifferential()` weights trait success by current stored energy, `StepSummary` centers mean-energy and legacy-trait snapshots, and `runExperiment()` aggregates only final summaries plus turnover analytics. That feedback loop cannot distinguish traits that sacrifice short-term energy for higher descendant output, delayed establishment, or downstream founding from traits that merely hoard energy without propagating, so optimization can drift toward state proxies rather than realized evolutionary success. Intervention: add lagged descendant-contribution and reproductive-value metrics by trait, lineage, and taxon, and report both immediate state proxies and delayed descendant outcomes in experiment aggregates. Dimension: Feedback Loops.
- [expand] [Life-History Compression] `Agent` state contains age, position, energy pools, taxonomy, and genome, but age only increments toward a hard `maxAge` kill switch while the same movement, harvest, encounter, and reproduction operators apply from birth to death. Within that architecture the system cannot express juvenile vs adult niches, maturation delay, reproductive senescence, dormant stages, or condition-dependent life-history switching; every lineage is forced into one immortal-until-cutoff adult template. Intervention: add explicit life-stage or size state with stage-specific metabolism, movement, harvest, encounter, and reproduction kernels, and let transitions depend on age, condition, and heritable parameters. Dimension: Expressiveness, Assumptions.
- [expand] [Single-Offspring Reproduction Kernel] `runReproductionPhase()` gives each eligible parent at most one reproduction attempt per tick, and `reproduceAgent()` always emits exactly one child funded by the fixed `offspringEnergyFraction`. No amount of planner tuning can produce clutch-size tradeoffs, brood splitting, semelparity vs iteroparity, many-small vs few-large offspring strategies, or reproductive restraint after a costly season because fecundity is structurally one-child-per-success. Intervention: replace the boolean reproduction gate with a reproductive allocation model that chooses offspring count and per-offspring investment under explicit energy budgets, then expose those allocations to mutation and selection. Dimension: Expressiveness, Assumptions.
- [expand] [Space Without Exclusion] The world stores occupancy as integer counts per cell, movement and settlement always choose among admissible coordinates without capacity checks, and cells can accumulate arbitrary co-occupancy before same-cell encounters resolve. That means the system cannot express territorial exclusion, shelter monopolization, crowding-driven blocking fronts, body-size-limited packing, or habitat engineering that works by denying physical access; space is only a weighted count field, not a contested substrate with admission constraints. Intervention: add per-cell occupancy capacity or footprint slots plus movement and settlement admission rules, and let contests for occupied sites depend on size, defense, or prior residency instead of only post-hoc co-occupancy encounters. Dimension: Expressiveness.
- [expand] [Globally Synchronous Forcing] `seasonalRegenMultiplierForTick()` and `seasonalFertilityContrastMultiplierForTick()` apply one shared sinusoidal phase to the whole map, while disturbance timing is also a single global schedule. Under this framework the environment can vary in magnitude but not in local timing, drift, or regime identity, so lineages cannot face asynchronous seasons, moving climate fronts, regional bad years, or persistent source-sink mosaics driven by out-of-phase forcing. Intervention: promote forcing into spatial fields with per-cell or per-region phase, amplitude, trend, and stochastic components, then feed those local signals into resource regeneration, fertility, disturbance, and agent observability. Dimension: Expressiveness, Assumptions.
- [expand] [Memory Horizon] Behavioral control currently exposes only `last_harvest_total` as transient memory, with no capacity for multi-tick history, rolling averages, or decay-weighted recollection. Policy thresholds (`movement_energy_reserve_threshold`, `movement_min_recent_harvest`, `reproduction_harvest_threshold`) can gate decisions based on immediate state, but the system cannot represent agents that track predation frequency over a temporal window, remember seasonal phase from prior cycles, adapt thresholds based on recent success rates, or condition strategies on time-since-founding. No amount of parameter tuning will enable temporal credit assignment, experience-weighted decision-making, or learning-like dynamics within an agent's lifetime because the architecture discards history beyond the current tick. Intervention: extend `internalState` with a fixed-capacity rolling buffer or decay-weighted state aggregator (e.g., exponentially weighted moving average over harvest, energy deltas, encounter outcomes), expose aggregate statistics (mean, variance, trend) as policy inputs, and allow policies to reference temporal features like "ticks since last reproduction" or "disturbance events in memory window." Dimension: Expressiveness, Feedback Loops.
- [expand] [Policy Search Space Locality] Behavioral policies use scalar thresholds (`movement_energy_reserve_threshold`, `reproduction_harvest_threshold`) with Gaussian mutation (`policyMutationMagnitude: 0.3`) that can arbitrarily perturb any value without regard to the current decision boundary or fitness landscape. A policy gating reproduction at `harvest_threshold = 15.0` can mutate to `14.7` (small functional change) or `5.3` (catastrophic regime shift) with equal probability, and mutation cannot infer whether a threshold is near-optimal (requiring fine-tuning) or far from any useful boundary (requiring large jumps). Under this architecture, adaptive search over policy space is a blind random walk with no locality bias, gradient signal, or meta-learning across generations; the system cannot exploit regularity in the policy-fitness landscape or accelerate discovery of effective thresholds. Intervention: add policy mutation operators that scale step size by local fitness variance (larger steps in flat regions, smaller steps near peaks), introduce correlation-driven adaptation (amplify parameters that historically co-vary with fitness), or implement a simple meta-parameter that tunes mutation magnitude per policy based on recent offspring success rates. Dimension: Evolutionary Mechanisms, Feedback Loops.
- [expand] [Metabolic Fungibility Violation] Agents store `energy`, `energyPrimary`, and `energySecondary` pools, but `agent-energy.ts:spendAgentEnergy()` and `addAgentEnergy()` enforce strict pool ordering: primary energy is always consumed first, secondary only after primary exhaustion, and incoming harvest is partitioned by fixed `genome.harvest` and `harvestEfficiency2` ratios. Within this architecture, agents cannot preferentially burn one substrate over another based on scarcity, stockpile secondary energy for disturbance insurance while spending primary for daily metabolism, or strategically convert energy pools during reproduction. The system also cannot represent substrate-specific metabolic efficiencies (e.g., lower cost to burn primary vs secondary) or condition-dependent fuel choice, so dietary specialization is reduced to a static harvest ratio baked into morphology rather than an adaptive metabolic strategy. Even if behavioral policies are extended to harvest allocation, they cannot control spending allocation or pool management, leaving a major axis of physiological strategy unexplorable. Intervention: separate energy acquisition from energy expenditure policies: add per-pool spending preferences or conditional burn rules (e.g., "spend secondary first if primary > threshold"), allow agents to set energy reserve targets by pool, and introduce per-substrate metabolic efficiency loci that scale the cost of using primary vs secondary energy for metabolism, movement, and reproduction. Dimension: Expressiveness, Assumptions.
- [expand] [Observability Blind Spots] `internalState` currently records `last_harvest_total` as a transient signal, but agents have no direct access to: their own age, their cell's disturbance history, recent encounter outcomes (energy stolen, energy lost), offspring survival in prior ticks, local taxonomic composition (species richness, clade diversity), or abiotic signals like fertility trend or seasonal phase. Movement decisions (`pickDestination`) score candidate cells by combined resource availability and habitat match, but that scoring is opaque to policy logic; policies can gate movement based on energy reserves but cannot read the reason the agent would move (resource scarcity vs crowding vs disturbance opening). Under this architecture, adaptive policies are blind to most ecological context that drives fitness, so contingent strategies remain locked to a tiny subset of available information. No amount of policy parameter tuning will enable age-dependent switching, disturbance-aware dispersal, or encounter-history-based aggression adjustment. Intervention: expand `internalState` or create a parallel `observations` map with read-only signals populated each tick: `age`, `local_crowding`, `local_species_richness`, `ticks_since_disturbance_at_cell`, `energy_stolen_last_tick`, `energy_lost_last_tick`, `offspring_survived_last_tick`, `current_fertility`, `seasonal_phase`. Make these signals first-class inputs to policy evaluation and expose them in policy fitness analytics. Dimension: Expressiveness, Feedback Loops.
- [validate] [Policy Fitness Attribution Granularity] `PolicyFitnessRecord` bins agents by `fertilityBin` (5 bins) and `crowdingBin` (5 bins), then tracks `harvestIntake`, `survived`, and `offspringProduced` conditional on policy presence. However, binning uses only the agent's initial state at tick start; agents that move during the turn, harvest from cells with varying fertility, experience disturbance mid-tick, or encounter heterospecifics are all collapsed into one ecological context label despite experiencing divergent selection pressures within the same timestep. The current approach also does not stratify by age, lineage founding status, post-disturbance phase, or seasonal cycle position, all of which plausibly modulate policy effectiveness. Under this framework, reported fitness decompositions conflate agents in fundamentally different ecological situations as long as they share coarse initial bins, so weak or spurious policy-outcome correlations may persist even when finer stratification would reveal strong conditional effects or context-dependent reversals. Intervention: add time-varying bin assignment (record bins at decision time, not tick start), introduce finer ecological stratification (age class, ticks-since-founding, disturbance phase, seasonal phase), and generate per-context fitness surfaces that compare policy-enabled vs policy-absent agents within each stratum. Report context-conditional effect sizes alongside aggregate correlations. Dimension: Feedback Loops, Temporal Credit Assignment.
- [expand] [Policy Activation Trigger Rigidity] Behavioral policies currently use hard thresholds: movement is blocked if `energy < movement_energy_reserve_threshold` OR `last_harvest < movement_min_recent_harvest`; reproduction is blocked if `last_harvest < reproduction_harvest_threshold`. These are binary gates with no probabilistic component, graded response, or multi-signal integration. An agent cannot implement "reproduce with 80% probability if harvest is high, 20% if marginal, 0% if low" or "move more cautiously (smaller neighborhood search) under low energy instead of freezing entirely." The system also cannot represent policies that combine signals (e.g., "block movement if energy < threshold AND crowding > threshold") or that adapt gate steepness (threshold sharpness) as a heritable parameter. Under this architecture, policy evolution is constrained to on/off switching points along single axes, precluding richer decision surfaces, risk-sensitive probabilistic strategies, or context-dependent threshold modulation. Intervention: replace binary threshold checks with continuous activation functions (sigmoid, linear ramp) parameterized by threshold and steepness, allow policies to specify multi-signal combination rules (weighted sums, min/max operators), and make activation function shape (steepness, offset) evolvable alongside threshold values. Dimension: Expressiveness.
- [refactor] [Policy-Genome Coupling] Behavioral policy parameters live in `agent.internalState` (a stringly-typed `Map<string, number>`), while morphological traits live in `agent.genomeV2.traits` (also a `Map<string, number>`). Both are heritable, both mutate, both contribute to fitness, but they are stored in separate data structures with separate mutation operators (`mutatePolicyParameters` vs `mutateGenomeV2`), separate observability pipelines (`PolicyObservabilitySummary` vs `genomeV2LociCount`), and separate distance metrics (none for policies vs `genomeV2Distance`). This architectural split creates duplication: two systems for heritable scalar state, two mutation functions, two sets of analysis tools. It also prevents policies from being treated as loci in speciation or cladogenesis distance calculations, so policy divergence cannot trigger taxonomic splits even if it drives ecological differentiation. The separation may have made sense during the initial behavioral-control spike to minimize coupling risk, but now that policies are fully heritable and integrated into the main loop, maintaining the boundary imposes technical debt and conceptual overhead. Intervention: unify policy parameters and genome traits into a single `GenomeV2.traits` map with per-locus metadata (mutation kernel, domain, observability tags), deprecate `internalState` for heritable state (retain only for true transient memory like `last_harvest`), and extend `genomeV2Distance`, speciation, and cladogenesis to include policy loci. Dimension: Assumptions, Meta-cognition.
- [validate] [Memoryless Null Fidelity for Behavioral Policies] The relabel-null baseline preserves clade birth counts and founding-time distributions but does not account for behavioral policy divergence. When comparing a policy-enabled run against its scrambled null, the procedure currently treats all clades as interchangeable regardless of whether they carry distinct heritable policies (e.g., high vs low `reproduction_harvest_threshold`). If policies drive ecological differentiation, such that clades with different thresholds occupy non-overlapping niches or disturbance regimes, then a null that ignores policy state will systematically underestimate the true contribution of behavioral adaptation to clade persistence and turnover. The current null also does not distinguish between clades founded with policies vs those that evolved policies later, or between clades where policies are active vs clades where policies exist but remain dormant. Intervention: extend the relabel-null family to include policy-matched nulls that preserve clade founding policy distributions, policy-activity-matched nulls that preserve the fraction of clades where policies actively gate decisions, and policy-stratified comparisons that report gains separately for high-policy vs low-policy lineages. Dimension: Feedback Loops, Temporal Credit Assignment.
- [expand] [Policy-Genome Architectural Duplication] Policy parameters (`policyState`) and morphological traits (`genomeV2.traits`) are stored in separate data structures, mutated by separate operators (`mutatePolicyParameters` vs `mutateGenomeV2`), observed via separate metrics (`PolicyObservabilitySummary` vs `genomeV2LociCount`), and excluded from shared operations like `genomeV2Distance()` and speciation / cladogenesis distance calculations. Both represent heritable scalar state subject to mutation and selection, yet the split creates duplication: two mutation functions, two observability pipelines, two distance systems. Critically, policy divergence cannot contribute to taxonomic splits even when it drives ecological separation, because policies are invisible to `genomeV2Distance()`. The split may have been justified during initial behavioral-control spike to minimize coupling risk, but now that policies are production-integrated, the boundary imposes technical debt, conceptual fragmentation, and missed opportunities for policies to participate in speciation and cladogenesis. Intervention: unify policy parameters and genome traits into a single `GenomeV2.traits` map with per-locus metadata (mutation kernel, domain, observability tags), deprecate `policyState` for heritable state (retain `transientState` only for true ephemeral memory like `last_harvest`), extend `genomeV2Distance`, speciation, and cladogenesis to include policy loci, and collapse the two mutation and observability systems into one. Dimension: Assumptions, Meta-cognition.
- [expand] [Energy Expenditure Policy Blindness] `spendAgentEnergy()` enforces a fixed spending rule: always burn energy in proportion to current pool sizes (primary and secondary), scaled by fixed per-substrate efficiency loci (`metabolic_efficiency_primary/secondary`). Agents cannot preferentially spend one substrate over another based on scarcity, stockpile secondary energy for disturbance insurance while spending primary for daily metabolism, or strategically convert pools during reproduction. The harvest allocation policy (`harvest_secondary_preference`) controls intake but has no influence over expenditure, leaving a major axis of physiological strategy unexplorable. Even if an agent evolves to harvest heavily from secondary resources, it cannot then protect those reserves by preferentially burning primary energy first. Under this architecture, dietary specialization remains incomplete: agents can choose what to eat but not what to burn, so metabolic strategy is half-blind. No amount of policy parameter tuning will enable fuel-switching, reserve management, or condition-dependent metabolic allocation because spending is hard-coded. Intervention: separate energy acquisition from energy expenditure policies: add per-pool spending preferences or conditional burn rules (e.g., "spend secondary first if primary > threshold"), allow agents to set energy reserve targets by pool, introduce spending-policy parameters to `policyState`, and let `spendAgentEnergy()` consult those policies instead of using only fixed proportional draw. Make spending policies heritable, mutable, and observable. Dimension: Expressiveness, Assumptions.
- [expand] [Mutation as Blind Random Walk] Behavioral policy mutation applies Gaussian noise (`policyMutationMagnitude: 0.3`) to threshold values without regard to the current fitness landscape, decision boundary steepness, or parameter sensitivity. A policy at `reproduction_harvest_threshold = 15.0` can mutate to `14.7` (minor functional shift) or `5.3` (catastrophic regime change) with equal probability. The system has no concept of local vs global search, no adaptive step-size adjustment based on recent fitness variance, and no memory of which parameters historically correlate with fitness gains. Mutation is also decoupled from outcome: a lineage that thrives due to a specific threshold cannot "learn" to make smaller mutations near that value or larger mutations when stuck in a fitness valley. Under this architecture, policy evolution is a memoryless coin-flip at each birth, with no exploitation of regularity in the policy-fitness surface and no meta-parameter adaptation over generations. The planner can tune the global `policyMutationMagnitude`, but that single scalar applies uniformly across all parameters, agents, lineages, and ecological contexts, regardless of local landscape geometry. Intervention: introduce mutation operators that scale step size by local fitness gradient or variance (larger steps in flat regions, smaller near peaks), add lineage-level or per-parameter meta-mutation rates that adapt based on recent offspring success, or implement simple correlation-driven mutation (amplify parameters that co-vary with fitness, dampen those that do not). Dimension: Evolutionary Mechanisms, Feedback Loops.
- [expand] [Context-Blind Policy Fitness] Policy fitness attribution bins agents by `fertilityBin` and `crowdingBin` at tick start, then aggregates outcomes (harvest, survival, offspring) across the entire timestep. Agents that move mid-tick, harvest from cells with varying fertility, experience disturbance after initial binning, encounter heterospecifics after settlement, or undergo age transitions during the turn are all collapsed into one ecological context label despite experiencing divergent selection pressures. The current scheme also ignores age, founding status, seasonal phase, disturbance phase, recent reproductive history, and energy reserve state, all of which plausibly modulate policy effectiveness. A movement threshold that works well for young, high-energy agents in post-disturbance open habitat may fail catastrophically for old, depleted agents in crowded pre-disturbance conditions, yet both will be pooled if they happen to start the tick in the same fertility / crowding bin. Under this framework, reported policy-outcome correlations are washed-out averages over heterogeneous micro-contexts, so weak aggregate effects may hide strong conditional advantages or context-dependent sign reversals. No amount of replication will reveal these unless stratification becomes finer and context assignment becomes dynamic. Intervention: record ecological context at decision time (not just tick start), add finer stratification dimensions (age class, ticks-since-founding, disturbance phase, seasonal phase, energy quartile), generate per-context fitness surfaces comparing policy-enabled vs policy-absent agents within each stratum, and report context-conditional effect sizes alongside aggregate correlations. Dimension: Feedback Loops, Temporal Credit Assignment.
- [expand] [Binary Policy Gates] Behavioral policies use hard thresholds: movement is blocked if `energy < threshold` OR `last_harvest < threshold`; reproduction is blocked if `last_harvest < threshold`. These are binary on / off switches with no graded response, probabilistic component, or multi-signal integration. An agent cannot implement "reproduce with 80% probability if harvest is high, 20% if marginal, 0% if low" or "move cautiously (smaller radius) under low energy instead of freezing entirely." The system also cannot represent policies that combine multiple signals with weighted sums, logical operators, or adaptive threshold steepness. Under this architecture, policy evolution is constrained to choosing breakpoints along single-axis step functions, precluding richer decision surfaces, risk-sensitive probabilistic strategies, or context-dependent threshold modulation. The fitness landscape over policy space is therefore full of discontinuous cliffs rather than smooth gradients, making adaptive search harder and evolutionary fine-tuning around near-optimal thresholds impossible. Intervention: replace binary threshold checks with continuous activation functions (sigmoid, linear ramp) parameterized by threshold location and steepness, allow policies to specify multi-signal combination rules (weighted sums, min / max, AND / OR logic), make activation function shape (steepness, inflection point) evolvable alongside threshold values, and add probabilistic gating. Dimension: Expressiveness, Evolutionary Mechanisms.
- [expand] [Fixed Mutation Candidate Catalog] `mutateGenomeV2()` draws new loci from a compile-time `candidateNewLoci` list (`DEFAULT_MUTATION_CANDIDATE_NEW_LOCI`), currently containing only human-enumerated entries like `habitat_preference`, `trophic_level`, `metabolic_efficiency_primary`, etc. The system can add or remove loci from this menu, but it cannot invent new kinds of loci, create parameterized locus families (e.g., "preference_for_X" for arbitrary substrate X), or mint loci with novel metadata (domain, bounds, mutation kernel). Under this architecture, the extensible genome is only extensible within a small pre-approved vocabulary; no amount of evolutionary time will produce trait types outside the human-defined catalog. The apparent open-endedness is an illusion: evolution explores a combinatorial space of presence / absence over a fixed set, not a generative space of trait invention. The same constraint applies to policy parameters: `POLICY_PARAMETER_KEYS` is hard-coded, so behavioral control cannot expand into new decision dimensions without human intervention. Intervention: replace fixed candidate lists with a trait-module factory or parameterized locus generator that can create new loci families at runtime, persist locus metadata (domain, bounds, mutation kernel, semantic tags) in a registry or as part of genome state, allow mutation to propose entirely novel locus names or types instead of only toggling predefined switches, and make the locus generator itself evolvable or emergent from lower-level building blocks. Dimension: Expressiveness, Open-Endedness.
- [expand] [Policy Observability Void for Movement and Reproduction] `PolicyObservabilitySummary` reports parameter prevalence and outcome correlations, and `PolicyActivationObservability` tracks `movementDecisionGatedFraction` and `reproductionDecisionGatedFraction`. However, the system does not record why a gate fired, which specific threshold was crossed, how often an agent was near the boundary, or what alternative action would have been taken without the policy. For movement, there is no signal distinguishing "blocked due to energy threshold" vs "blocked due to harvest threshold" vs "moved anyway because thresholds were not met." For reproduction, there is no count of would-be births suppressed by policy vs policy-enabled births under marginal conditions. The current observability also does not track policy effectiveness conditional on gate activation: do agents that moved despite low energy fare worse than those that stayed put? Without these signals, the feedback loop cannot distinguish policies that gate frequently but harmlessly from policies that gate rarely but critically, or policies that create advantage by enabling risky actions vs suppressing harmful ones. Intervention: add decision-level observability that records threshold-crossing events, near-miss counts, gate-specific activation, counterfactual action traces, and outcome stratified by gate-firing vs gate-dormant states. Surface these in `PolicyObservabilitySummary` and study exports. Dimension: Feedback Loops.
- [validate] [Harvest Allocation Feedthrough to Fitness] The March 22, 2026 harvest allocation smoke test (`behavioral-harvest-allocation-smoke.ts`) demonstrated that `harvest_secondary_preference` successfully redirects intake across resource layers (policy carriers ended with higher secondary-energy share than controls). However, the test ran with reproduction disabled (`reproduceProbability: 0`) and only 40 ticks, so it does not validate whether harvest allocation creates fitness advantage under live selection. The current fitness attribution pilot (`policy-fitness-refined-attribution-pilot.ts`) was run before harvest policies existed, so there is no causal readout yet for whether secondary-preference tuning improves survival, energy gain, or reproductive output in matched ecological contexts. Under the present evidence base, harvest allocation is a confirmed control surface but an unvalidated adaptive surface. Intervention: run a matched-context fitness attribution study with harvest policies enabled, stratifying by fertility bin, crowding bin, and secondary-resource availability bin, then report per-policy-parameter correlations with harvest intake, survival rate, and offspring production. Compare policy-enabled vs policy-absent agents within each stratum. If the signal is weak or negative, diagnose whether that reflects poor default parameter values, ecological mismatch, or fundamental design problems. Dimension: Feedback Loops, Temporal Credit Assignment.
